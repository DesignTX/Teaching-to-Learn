<% provide :head_tags do %>
  <meta name='turbolinks-visit-control' content='reload'>
  <script>
    document.addEventListener("DOMContentLoaded", function(){
      initMap(<%=@post.latitude%>, <%=@post.longitude%>)
    });
  </script>
<% end %>

<br>
<br>
<br>
<div class="container">
  <div class="jumbotron">
    <h1 class="display-4"><%=@post.title%></h1>
  <br>
    <div class="card w-75">
        <div class="card-body">
          <h5 class="card-title"><%=@post.user.username%></h5>
             <hr>
             <%= image_tag(@post.user.avatar.service_url) if @post.user.avatar.attached? %>
          <p class="card-text">Author: <%=@post.user.firstname%> | <%=@post.user.lastname%> </p>
          <p>posted: <%=@post.created_at%> </p>

        </div>
    </div>

    <hr class="my-4">
    <p><%=@post.content%></p>
    <div class="text-center">
       <%= image_tag(@post.image.service_url) if @post.image.attached? %>
    </div>
   

    <% if can? :update, @post %>
    <%= link_to "edit", edit_post_path(@post.id), class: "btn btn-primary" %> |
    <% end %>

    <% if can? :destroy, @post %>
    <%= link_to "delete", post_path(@post.id), method: :delete, class: "btn btn-danger"  %>
    <% end %>

<div>
<hr>
<div class="text-center">
<p>If you are interest in getting into contact with the person conduction this lesson send them an email</p>
<%= link_to "email", email_path(@post), class: "btn btn-success"  %>
</div>


<div class="container">
  <p>
    <div id="map"></div>
  </p>
</div>
</div>
  </div>
  <%= render 'layouts/shared/stripe_form' %>
  <br>
  <br>

<div class="card text-center">
    <div class="card-header">
      <h2>Comments</h2>
    </div>
    <div class="card-body"
    >
      <article>
    <h3><% @comments.each do |comments| %> </h3> 
        <div id="single-comment">
        <h6><%= comments.comment %></h6>
          <% if can? :update, @comment %>
          <%= link_to "edit", edit_comment_path(@post.id), class: "btn btn-primary" %> 
          <% end %>

          <% if can? :destroy, @comment %>
          <%= link_to "delete", comment_path(@post.id), method: :delete, class: "btn btn-danger"  %>
          <% end %>
        <% end %>
        </div>
      </article>
            <%= form_for @comment, url: comments_path ,html: {class: "edit-form"}, :action => "create" do |f| %>
         <%= f.label :comment %>
        <%= f.text_area :comment, class: "text-area" %>
        <%= f.hidden_field(:post_id, :value => @post.id )%>
        <%= f.submit 'Create', class: 'btn btn-primary' %>
      <% end %>
        
      <article>
      </article>
    </div>

  </div>



















    </div>


<hr>














